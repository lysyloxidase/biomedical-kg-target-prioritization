# Copy to .env only when using docker/docker-compose.yml with Neo4j.
# Replace this placeholder with a strong local secret. Never commit .env.
NEO4J_USER=neo4j
NEO4J_PASSWORD=replace-with-a-strong-local-password

# The API is fail-closed by default and requires validated artifacts.
KGTP_DEMO_MODE=false
